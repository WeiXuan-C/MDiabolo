# MDiabolo Scoring App 测试流程文档

这份文档用来做手动验收测试。测试目标不是只看页面能不能打开，而是从真实比赛现场的角度确认：管理员能建赛事和比赛、能添加人员、能安排回合、裁判能评分、排名能生成、数据能离线保存和 QR 同步。

## 0. 测试前准备

### 浏览器开发模式

1. 打开 PowerShell。
2. 进入项目目录：

```powershell
cd C:\Users\weixu\MDiabolo
```

3. 启动项目：

```powershell
npm run dev
```

4. 浏览器打开：

```text
http://127.0.0.1:3000/
```

预期结果：

- 页面进入 MDiabolo 首页。
- 如果出现“正在载入离线比赛数据库”，几秒内应该进入首页。
- 顶部只有 4 个功能按钮：导出、导入、语言、字体/简易模式。

### Android APK 测试

1. 安装 `MDiabolo-release-latest.apk` 或 `MDiabolo-debug-latest.apk`。
2. 打开 App。
3. 第一次打开时允许相机权限，方便后续 QR 同步测试。

预期结果：

- App 可以进入首页。
- 手机返回键不会直接退出；应该优先回到上一页，连续返回才询问/退出。

## 1. 首页与显示设置

1. 在首页确认显示“当前赛事”和“选择你的身份”。
2. 点击顶部语言按钮。
3. 依次切换：
   - 双语
   - 只中文
   - 只英文
4. 点击字体按钮。
5. 调整字体大小。
6. 打开/关闭简易模式。

预期结果：

- 双语模式下，中文是主要文字，英文更小、更浅色。
- 只中文/只英文时，不应该留下多余的点号或空白。
- 简易模式打开后，说明文字会隐藏，但按钮和核心表单仍可操作。
- 往下滑动页面时，顶部 Logo 和 MDiabolo 标题区会收起，只保留操作按钮。

## 2. 管理员注册与登录

1. 首页点击“管理员”。
2. 如果还没有管理员账号，进入注册本机管理员。
3. 输入：

```text
管理员姓名：admin
密码：123456
```

4. 点击注册/登录。
5. 如果已经有账号，直接输入管理员姓名和密码登录。

预期结果：

- 登录成功后进入“比赛控制台”。
- 可以看到上方的赛事、比赛、回合下拉选择。
- 底部/导航栏有 6 个功能：回合、对阵图、排名、人员、同步、设置。

记录失败：

- 如果不能登录，记录错误提示文字。
- 如果注册后刷新页面账号消失，说明本地保存异常。

## 3. 新增赛事

1. 管理员进入“设置”。
2. 找到“赛事主题”。
3. 在新增赛事输入框填写：

```text
新赛事华文名字：MDiabolo 测试杯 2026
新赛事英文名字：MDiabolo Test Cup 2026
```

4. 点击“新增赛事”。
5. 找到“首页当前赛事”。
6. 将当前赛事切换成刚刚新增的赛事。
7. 回到首页。

预期结果：

- 新赛事出现在赛事列表中。
- 首页“当前赛事”显示刚刚新增的赛事。
- 刷新浏览器或重开 App 后，新赛事仍然存在。

## 4. 新增比赛项目

1. 管理员进入“设置”。
2. 确认上方“赛事”下拉选择的是刚刚新增的赛事。
3. 找到“比赛项目”。
4. 新增比赛：

```text
新比赛华文名字：公开双人组
新比赛英文名字：Open Pair
```

5. 点击“新增比赛”。
6. 将比赛状态设为：

```text
Draft / 草稿
```

7. 将比赛类型设为：

```text
Duo/Team Stage / 双人或团体舞台赛
```

预期结果：

- 新比赛出现在比赛项目列表中。
- 上方“比赛”下拉可以选择这个比赛。
- 比赛状态可以在 Draft、Active、Completed 之间切换。

重要检查：

- 裁判端只会看到 `Active / 进行中` 的比赛。
- 如果比赛还是 Draft，裁判端应该不会拿到可评分比赛。

## 5. 添加选手

1. 管理员上方选择刚刚新增的赛事和比赛。
2. 进入“人员”。
3. 在“运动员”区域新增至少 3 位选手：

```text
华文名字：选手一
英文名字：Athlete One

华文名字：选手二
英文名字：Athlete Two

华文名字：选手三
英文名字：Athlete Three
```

4. 每新增一位后点击“添加”。
5. 使用搜索框搜索 “选手二” 或 “Athlete Two”。
6. 使用排序下拉切换按出场顺序、按姓名、按国家、按学校。

预期结果：

- 新增后选手马上出现在当前比赛的运动员列表。
- 搜索可以找到中文名和英文名。
- 排序切换不会让列表错乱或按钮跳动。
- 刷新页面后选手仍然存在。

## 6. 添加裁判并分配到比赛

1. 管理员仍在“人员”。
2. 在“裁判”区域新增至少 2 位评分裁判和 1 位技术裁判：

```text
华文名字：评分裁判一
英文名字：Judge One
类型：评分

华文名字：评分裁判二
英文名字：Judge Two
类型：评分

华文名字：技术裁判
英文名字：Technical Judge
类型：技术
```

3. 每新增一位后点击“添加”。
4. 回到首页，点击“我是裁判”。

预期结果：

- 新裁判会出现在裁判选择页面。
- 选择裁判后，如果比赛仍是 Draft，应该显示没有分配给你的进行中比赛。
- 这说明裁判分配和比赛状态限制生效。

重要说明：

- 在当前实现里，裁判是在“人员”页新增或从其他比赛加入时被分配到当前比赛。
- 如果某位裁判没有出现在某个比赛的裁判端，先检查管理员上方选择的比赛是否正确，再检查该裁判是否在该比赛“人员 > 裁判”列表里。

## 7. 设置回合和参赛名单

1. 管理员进入“回合”。
2. 确认上方选择正确赛事、比赛、回合。
3. 在回合流程中选择第一回合。
4. 找到“参赛名单”。
5. 点选刚刚新增的 3 位选手，把他们加入本回合。
6. 使用回合名单搜索框搜索其中一位选手。
7. 使用排序下拉切换：
   - 按出场顺序
   - 已选择优先
   - 按姓名
   - 按国家
   - 按学校
8. 将第一回合状态改为：

```text
Active / 进行中
```

9. 回到“设置”，将比赛状态改为：

```text
Active / 进行中
```

预期结果：

- 本回合参赛人数显示为 3。
- 选手加入/移出时有明确反馈。
- 只有回合和比赛都为 Active 时，裁判端才能评分。

## 8. 裁判评分流程

1. 回到首页。
2. 点击“我是裁判”。
3. 选择“评分裁判一”。
4. 在裁判页确认：
   - 比赛下拉显示刚刚新增的比赛。
   - 回合下拉显示第一回合。
   - 进度卡显示本回合共 3 位选手，已评分 0，剩余 3。
5. 点击第一位选手。
6. 填入每个评分项目，确保都在允许范围内。
7. 点击“保存评分”。
8. 返回选手列表。
9. 重复评分第二位和第三位选手。

预期结果：

- 保存按钮有 loading 或已保存反馈。
- 已评分选手会显示完成标记。
- 进度卡从 0/3 变成 3/3。
- 必填完成显示“是”。
- 刷新页面后评分仍然存在。

失败检查：

- 如果保存失败，记录顶部错误提示。
- 如果进度没有变化，检查当前裁判、比赛、回合是否选对。

## 9. 技术裁判记录失误

1. 回到首页。
2. 点击“我是裁判”。
3. 选择“技术裁判”。
4. 选择同一个比赛和回合。
5. 点击第一位选手。
6. 使用加号增加失误次数。
7. 点击“保存”。
8. 返回选手列表。

预期结果：

- 技术裁判看到的是失误次数计数器，不是评分项目。
- 扣分合计会根据“设置 > 技术失误规则”的每次扣分自动计算。
- 保存后有明确反馈，选手显示完成。

## 10. 排名与导出

1. 管理员登录。
2. 进入“排名”。
3. 确认选择正确比赛和回合。
4. 查看排名列表。
5. 使用搜索框搜索选手。
6. 使用排序下拉切换：
   - 最终排名
   - 出场顺序
   - 姓名
   - 席次点数/积分
   - 平均分
   - 完成裁判数
7. 点击“导出 Excel”。
8. 点击“导出 PDF”。

预期结果：

- 排名能显示选手、完成裁判数、平均分、排名等信息。
- 导出按钮点击后有“已导出”反馈。
- 下载文件内容对应当前比赛和回合。

注意：

- 如果只有一个评分裁判完成，排名可能仍然显示，但应视为未完整。
- 正式测试建议至少两个评分裁判都完成所有选手。

## 11. QR 导出和粘贴导入

这一步用于测试离线数据同步。建议准备两台设备，或一个浏览器普通窗口加一个无痕窗口。

### A 设备导出

1. 在 A 设备完成赛事、比赛、人员、评分。
2. 点击顶部“导出 QR”按钮。
3. 确认出现“同步 QR”弹窗。
4. 点击“复制导入数据”。

预期结果：

- 复制成功后有提示。
- 如果数据很多，QR 会自动轮播。
- 弹窗中有可复制的导入数据文本。

### B 设备导入

1. 在 B 设备打开 App。
2. 点击顶部“导入 QR”按钮。
3. 将 A 设备复制的数据贴到文本框。
4. 点击“粘贴导入”。
5. 查看“确认 QR 同步”页面。
6. 点击“查看整合详情”。
7. 确认新增、相同、冲突、删除数量。
8. 如果有冲突，查看本机版本和 QR 版本差异。
9. 点击“确认导入”。

预期结果：

- 导入前不会直接覆盖数据，而是先显示预览。
- 新增了几位选手、几位裁判、几笔成绩应清楚显示。
- 冲突时能看到字段差异。
- 重复导入同一包数据时，应提示“这包 QR 已经整合过”，不应该再次要求确认。

## 12. QR 相机扫码导入

1. A 设备打开“导出 QR”。
2. B 设备点击“导入 QR”。
3. 点击“打开相机扫码”。
4. 将 B 设备相机对准 A 设备 QR。
5. 保持稳定直到读取完成。
6. 查看同步预览并确认导入。

预期结果：

- 打开相机后能看到扫描提示。
- 第一帧等待时间不应过长。
- 多页 QR 会逐页读取，进度应显示已扫到几页。
- 扫描完成后自动停止相机并进入同步预览。

失败记录：

- 如果一直停在“等待第一帧”，记录手机型号、Android 版本、是否已授权相机、是否能看到系统相机权限图标。
- 如果扫到一半失败，记录当前显示的页数和错误提示。

## 13. 管理员同步记录

1. 管理员进入“同步”。
2. 找到“本机同步身份”。
3. 填写：

```text
设备名称：Admin Phone A
导出者：现场管理员
```

4. 做一次 QR 导出和导入。
5. 回到“同步”页查看“QR 整合记录”。

预期结果：

- 整合记录显示来源设备名称。
- 整合记录显示导出者。
- 整合记录显示导出时间。
- 可以看到本次整合新增、接受修改、删除、保留本机的数量。

## 14. 离线保存测试

1. 在手机上打开 App。
2. 关闭 Wi-Fi 和移动网络，或打开飞行模式。
3. 裁判端保存一笔评分。
4. 关闭 App。
5. 重新打开 App。
6. 回到同一裁判、同一比赛、同一回合、同一选手。

预期结果：

- 断网时仍然可以保存。
- 重开 App 后评分仍然存在。
- 管理员排名页能看到这笔评分。
- 顶部不再需要云状态按钮也不影响离线保存。

## 15. 回退键测试

Android 真机测试：

1. 首页进入管理员。
2. 登录后进入“人员”。
3. 点击手机系统返回键。
4. 再进入裁判页，选择选手评分页面。
5. 点击系统返回键。
6. 在首页连续点击返回键。

预期结果：

- 在深层页面按返回键应回到上一步。
- 不应该第一次返回就直接退出 App。
- 首页连续返回时才退出或询问退出。

## 16. 最小完整验收清单

测试完成前，至少确认下面项目全部通过：

- [ ] App / 浏览器可以进入首页。
- [ ] 管理员可以注册和登录。
- [ ] 可以新增赛事。
- [ ] 可以新增比赛项目。
- [ ] 比赛状态可以设为 Draft、Active、Completed。
- [ ] 可以新增选手并分配到当前比赛。
- [ ] 可以从其他比赛多选加入现有选手，并能看到来源比赛。
- [ ] 可以新增评分裁判和技术裁判并分配到当前比赛。
- [ ] 第一回合/预赛默认当前比赛全部选手参赛，点击后可取消或重新加入。
- [ ] 可以把选手加入指定回合。
- [ ] Draft 比赛或 Draft 回合不会开放给裁判评分。
- [ ] 未到比赛日期时间时，Active 回合也不会开放给裁判评分。
- [ ] Active 比赛和 Active 回合会出现在裁判端。
- [ ] 评分裁判可以保存所有选手评分。
- [ ] 技术裁判可以保存失误次数。
- [ ] 裁判端进度卡正确显示总人数、已完成、剩余、必填完成。
- [ ] 裁判保存后会返回选手名单，已评分选手显示绿色分数数字。
- [ ] 管理员排名页能看到结果。
- [ ] 按排名晋级下一轮有处理中、成功或失败反馈。
- [ ] Excel / PDF 可以导出，并显示下载文件名和 Downloads 提示。
- [ ] QR 导出可以复制 brotli `MDACTB/MDACTBP` 导入数据。
- [ ] 粘贴导入会先显示同步预览。
- [ ] 冲突会显示差异。
- [ ] 重复导入同一包数据会被拦截。
- [ ] 管理员同步页能看到整合记录、来源设备、导出者、导出时间。
- [ ] 离线状态下保存数据，重开后数据仍存在。

## 17. 最近聊天更新回归测试

这一节专门用来确认最近聊天里提到的新 bug / 新需求是否已经解决。每次重新打包 APK 后，建议至少跑一次这一节。

### 17.1 Brotli Paste Import / Export

状态：已实现。

1. 在 A 设备点击顶部“导出 QR”。
2. 点击“复制导入数据”。
3. 将复制出来的文本粘贴到任意文本框检查开头。
4. 在 B 设备点击顶部“导入 QR”。
5. 粘贴这段数据并点击“粘贴导入”。

预期结果：

- 新导出的同步文本应以 `MDACTB|` 或 `MDACTBP|` 开头。
- action log 和 snapshot 都会保留在同一包数据里。
- 旧 action-sync 格式 `MDACTZ|` / `MDACTZP|` 不再作为新版本兼容目标。
- 大型数据包应明显比旧版小；30 比赛、900 选手、120 裁判、3600 成绩、900 fault 的测试包约为 67 KB。
- 粘贴导入后仍会先显示同步预览，不会直接覆盖。

可直接测试的数据：

```text
C:\Users\weixu\MDiabolo\test-data\realistic-30x30x4-brotli-paste-import.txt
```

### 17.2 从其他比赛加入选手

状态：已实现。

1. 创建两个比赛 A 和 B。
2. 在比赛 A 新增几位选手。
3. 切换到比赛 B。
4. 进入“人员 > 运动员”。
5. 在“从其他比赛加入现有运动员”区域多选比赛 A 的选手。
6. 点击加入本比赛。

预期结果：

- 可以一次选择多位选手。
- 选项文字会显示选手来自哪个比赛。
- 加入后选手出现在当前比赛。
- 加入后默认进入第一回合名单。

### 17.3 预赛参赛名单默认全员参赛

状态：已实现。

1. 在某个比赛新增 3 位选手。
2. 进入“回合”页，选择第一回合/预赛。
3. 查看“参赛名单”。
4. 点击其中一位选手取消参赛。
5. 再点击同一位选手重新加入。

预期结果：

- 第一回合第一次打开时，当前比赛所有选手默认已参赛。
- 点击选手会取消参赛，再点一次会重新加入。
- 用户手动调整后，系统不会再次偷偷把已取消的选手加回来。

### 17.4 Judge 保存后返回名单并显示分数

状态：已实现。

1. 进入裁判端。
2. 选择一个已开始且 Active 的比赛/回合。
3. 点击一位选手进入评分页。
4. 填完分数并保存。

预期结果：

- 保存按钮有 loading / 已保存反馈。
- 保存成功后自动回到选手名单。
- 已评分选手右侧显示绿色数字分数，例如 `104.0`。
- 不再只显示绿色勾。
- 技术裁判保存 fault 后显示扣分数字，例如 `-0.5` 或 `0.0`。

### 17.5 按排名晋级下一轮反馈

状态：已实现。

1. 至少创建两个回合。
2. 让所有必要评分裁判完成当前回合评分。
3. 管理员进入“回合”页。
4. 点击“按排名晋级下一轮”。

预期结果：

- 按钮会显示处理中状态。
- 成功后显示已晋级反馈。
- 当前回合变成 Completed。
- 下一回合变成 Active，并填入晋级选手。
- 如果没有下一回合，会提示先建立下一回合。
- 如果评分不完整，会提示所有评分裁判完成后才能晋级。

### 17.6 比赛开始前不能评分，时间包含日期

状态：已实现。

1. 管理员进入“回合”页。
2. 将比赛状态设为 Active，回合状态也设为 Active。
3. 设置“比赛日期时间”为未来时间，例如明天上午 9:00。
4. 进入对应裁判端。

预期结果：

- 裁判端可以看到该比赛/回合，但不能进入评分。
- 页面显示比赛还没开始，并显示开始日期时间。
- 时间输入框包含日期和时间，不再只是小时/分钟。
- 到达开始时间后，裁判端才允许评分。

### 17.7 Excel / PDF 导出反馈

状态：已实现。

1. 管理员进入“排名”页。
2. 点击“导出 Excel”。
3. 点击“导出 PDF”。

预期结果：

- 两个按钮都有导出中/已导出反馈。
- 成功后顶部提示会显示文件名。
- 提示会说明到浏览器或手机 Downloads 查看。
- PDF 不应静默无反应；如果失败应显示错误提示。
- 导出的表格里比赛时间显示日期 + 时间。

### 17.8 当前仍需留意

- Brotli WASM 会作为动态 chunk 加载，第一次导出/导入大型数据时可能会比普通操作慢一点。
- Build 会出现 Vite 大 chunk warning，这是 brotli WASM 和既有大依赖导致；目前不影响 APK 打包。
- `npm audit` 仍会报告 `xlsx` 的 high advisory，npm 当前没有可自动修复版本。这个风险与本次 brotli 改动无关，但后续如果要处理导入 Excel 或不可信 Excel 文件，需要单独评估。

## 18. Bug 记录模板

发现问题时，建议用这个格式记录：

```text
测试日期：
测试人：
设备/浏览器：
App 版本或 APK 文件名：

问题标题：
前置条件：
操作步骤：
实际结果：
预期结果：
截图/录屏：
是否可重复：
备注：
```
